{
  "name": "darius-codes",
  "version": "1.0.0",
  "description": "Blog thingy of Darius",
  "scripts": {
    "sass": "sass --style=compressed src/_scss/main.scss _site/styles.css",
    "watch:eleventy": "eleventy --serve",
    "watch:sass": "npm run sass -- --watch",
    "dev": "npm-run-all sass --parallel watch:*",
    "build": "npm run sass && eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^0.11.0",
    "@11ty/eleventy-plugin-rss": "^1.0.9",
    "npm-run-all": "^4.1.5",
    "sass": "^1.26.10"
  }
}
